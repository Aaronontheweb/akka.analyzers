name: pr_validation

on:
  push:
    branches:
      - master
      - dev
      - main
  pull_request:
    branches:
      - master
      - dev
      - main

jobs:
  test:
    name: Test-${{matrix.os}}
    runs-on: ${{matrix.os}}

    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: "Checkout"
        uses: actions/checkout@v4.1.1
        with:
          lfs: true
          fetch-depth: 0

      - name: "Install .NET SDK"
        uses: actions/setup-dotnet@v4.0.0
        with:
          global-json-file: "./global.json"

      - name: "Update release notes"
        shell: pwsh
        run: |
          ./build.ps1

      - name: "dotnet build"
        run: dotnet build -c Release

      # .NET Framework tests can't run reliably on Linux, so we only do .NET 8

      - name: "dotnet test"
        shell: bash
        run: |
          if [ "${{ matrix.os }}" = "ubuntu-latest" ]; then
            dotnet test -c Release --framework net8.0
          else
            dotnet test -c Release
          fi

      - name: "dotnet pack"
        run: dotnet pack -c Release -o /bin/nuget

      - name: "Create .NET Framework Console App and Install Akka.Analyzers"
        if: matrix.os == 'windows-latest'
        shell: pwsh
        run: |
          # Create a new .NET Framework 4.8 console application
          dotnet new console -o DotNetFrameworkApp -f net48
          cd DotNetFrameworkApp
          
          # Install Akka.Analyzers from the local NuGet source
          dotnet nuget add source ../bin/nuget -n local
          dotnet add package Akka.Analyzers --source local